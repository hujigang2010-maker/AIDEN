/** app.wxss — NYU Violet 气质：高大上紫调 */
page {
  --primary: #57068c;
  --teal: #8900e1;
  --gold: #c5a572;
  --bg: #f5f1f8;
  --card: #ffffff;
  --text: #1c1226;
  --muted: #6b6178;
  --line: #e4dcec;
  --danger: #c0392b;
  --violet-deep: #330662;
  background: var(--bg);
  color: var(--text);
  font-family: -apple-system, BlinkMacSystemFont, "PingFang SC", "Noto Sans SC",
    "Helvetica Neue", sans-serif;
  font-size: 28rpx;
  line-height: 1.55;
}

.container {
  padding: 24rpx 28rpx 48rpx;
  box-sizing: border-box;
}

.section-title {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  margin: 36rpx 0 20rpx;
}

.section-title .h {
  font-size: 34rpx;
  font-weight: 650;
  color: var(--primary);
  letter-spacing: 1rpx;
}

.section-title .more {
  font-size: 24rpx;
  color: var(--teal);
}

.card {
  background: var(--card);
  border-radius: 16rpx;
  padding: 28rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 8rpx 24rpx rgba(18, 63, 85, 0.04);
}

.tag {
  display: inline-block;
  padding: 4rpx 14rpx;
  border-radius: 6rpx;
  font-size: 22rpx;
  line-height: 1.4;
}

.tag-teal {
  background: rgba(11, 117, 132, 0.12);
  color: var(--teal);
}

.tag-gold {
  background: rgba(161, 124, 76, 0.15);
  color: var(--gold);
}

.tag-free {
  background: rgba(30, 122, 74, 0.12);
  color: #1e7a4a;
}

.tag-paid {
  background: rgba(192, 57, 43, 0.1);
  color: var(--danger);
}

.btn-primary {
  background: linear-gradient(135deg, #330662, #57068c 55%, #8900e1);
  color: #fff;
  border-radius: 999rpx;
  text-align: center;
  padding: 22rpx 0;
  font-size: 30rpx;
  font-weight: 600;
}

.btn-ghost {
  border: 2rpx solid var(--primary);
  color: var(--primary);
  border-radius: 999rpx;
  text-align: center;
  padding: 20rpx 0;
  font-size: 28rpx;
}

.muted {
  color: var(--muted);
  font-size: 24rpx;
}

.price {
  color: var(--danger);
  font-weight: 650;
}
